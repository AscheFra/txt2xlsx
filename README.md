# txt2xlsx

Konvertierungswünsche:

ob der Header bleibt oder nicht , ist mir egal
die Spaltenköpfe sollten so bleiben
die Spalte Time(s) kann weg, dafür könnte die Date -Spalte um die Sekundenanzeige erweitert werden
der erste Datensatz sollte erst bei Änderung des Werts in der Error [g/h] – Spalte aufgenommen werden, würde auch mit der Crystal length [mm] -Spalte funktionieren
Eingabe bei Start, jeder wievielte Datenpunkt übernommen werden soll
